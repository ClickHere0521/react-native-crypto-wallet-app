# Zluck React Native
```
1) git clone 

2) cd <ProjectName>

3) yarn

5) yarn global add react-native-rename
                  or
  npm install react-native-rename -g

6) npx react-native-rename <ProjectName> -b <bundleIdentifier>

7) react-native link

8) cd ios && pod install && cd ..
```


##Purpose
``` 
 this app is for crypto wallet, and users can make a transaction with their cryptocurrency.